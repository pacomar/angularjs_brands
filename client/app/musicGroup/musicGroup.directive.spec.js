'use strict';

describe('Directive: musicGroup', function () {

  // load the directive's module and view
  beforeEach(module('angularjsBandsApp'));
  beforeEach(module('app/musicGroup/musicGroup.html'));

  var element, scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  element = element;
});
